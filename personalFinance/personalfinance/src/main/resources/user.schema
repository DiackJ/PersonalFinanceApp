CREATE TABLE user (
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(50),
    password VARCHAR(255),
    name TEXT,
    monthly_income INT,
    current_balance INT,
    total_expenses INT
   );